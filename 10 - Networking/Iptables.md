Ver reglas definidas --> `iptables -L`
Ver formato de reglas definidas --> `iptables -s`
Borrar reglas -->
> `iptables --flush`
> `iptables --table-nat --flush`
> `iptables --delete-chain`
> `iptables --table-nat --delete-chain`

